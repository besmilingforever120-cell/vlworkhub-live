NODE_ENV=development
JWT_SECRET=change-me
COOKIE_DOMAIN=
API_PORT=8080
NEXT_PUBLIC_API_URL=http://localhost:8080
NEXT_PUBLIC_ROOT_URL=http://localhost:3000
NEXT_PUBLIC_CARE_URL=http://localhost:3001
NEXT_PUBLIC_HR_URL=http://localhost:3002
NEXT_PUBLIC_URSAFE_URL=http://localhost:3003

# Local Windows development example
# Use this when running the API against a PostgreSQL instance reachable on localhost.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/vlworkhub

# Ubuntu Docker deployment example
# Use this when the API and PostgreSQL run together in docker compose.
# DATABASE_URL=postgres://postgres:postgres@postgres:5432/vlworkhub
